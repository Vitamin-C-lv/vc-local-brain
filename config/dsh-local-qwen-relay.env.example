# Production inspection record:
# `systemctl --user cat dsh-local-qwen-relay` shows a transient unit with an
# empty Environment= and no EnvironmentFile=. This file is a credential-free
# review/deployment template and is not attached to the live unit.
QWEN_UPSTREAM=http://172.25.240.1:17861
QWEN_RELAY_DEBUG=0
QWEN_RESTART_HELPER=/mnt/d/VC-AI-Pet/runtime/Start-LocalQwen.ps1
